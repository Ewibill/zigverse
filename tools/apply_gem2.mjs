#!/usr/bin/env node
/* apply_gem2.mjs - DUAL GEM (ZigWebGPU 0.47.0)  ·  2026-09-20
 *
 * A SECOND stone lines the cupped INTERIOR while opts.gem keeps the shell.
 * The stones differ in dispersion, facet and spark as well as colour, so
 * diamond outside + emerald inside is hard spectral fire on the shell and a
 * soft included glow within - two optical behaviours on two faces of one shard.
 *
 * WHY IT IS SMALL: gemBlock(G, cond) was ALREADY parameterised by stone and by
 * face condition and invoked once. This is a second invocation at the same
 * splice point. No new bindings, no new varyings, nothing that can change an
 * auto-derived layout - which is the 0.46.0 fault it structurally cannot repeat.
 *
 * THE TRAP THIS CLOSES: gemHueRot/gemSky were declared only `if (GEM)`. A second
 * stone with no first would emit a block calling undeclared WGSL functions.
 * nvidia tolerates a dangling reference; Metal refuses it and the pipeline fails
 * to build - black canvas, healthy HUD, 165fps. That is 0.44.1 exactly.
 * They are now gated on GEM || GEM2, and test/gem2_ref.mjs case C proves it.
 *
 * TOUCHES   engine/zigwebgpu.js  species/sickleswarm.js  zigverse_engine.html
 * ADDS      test/gem2_ref.mjs
 *
 *   node tools/apply_gem2.mjs --dry-run
 *   node tools/apply_gem2.mjs
 *
 * Every anchor must match EXACTLY ONCE across every file or NOTHING is written.
 * .bak written per file. Re-running after success is a no-op.
 */
import fs from "node:fs";
import crypto from "node:crypto";

const DRY = process.argv.includes("--dry-run");
const sha = (s) => crypto.createHash("sha256").update(s).digest("hex");
const ok = (m) => console.log("  OK    " + m);
const bad = (m) => console.log("  FAIL  " + m);

const EDITS = JSON.parse("[{\"file\": \"engine/zigwebgpu.js\", \"old\": \"  const ZigWebGPU = global.ZigWebGPU || (global.ZigWebGPU = {});\\n  ZigWebGPU.VERSION = \\\"0.46.0\\\";   // 0.46.0: GROUND IS WIRED \\u2014 a declared ground now sets the sky triple, the three scene clear values (NOT the trail's two, where black means zero), the afterimage's compositing/decay/gate, and the Radiance room, from one word. Signed compositing is SPLICED, so `void` and `dusk` emit the base afterimage byte for byte. THE FAULT THAT COST 8/18: the base blitFs never used the uniform, so `layout:\\\"auto\\\"` gave it a 2-entry layout; the signed blitFs uses A.lift, the layout became 3, and every bind group was rejected \\u2014 1,240 driver errors per run at 165fps with a black screen. A shader splice that changes WHICH BINDINGS A STAGE USES changes its auto-derived layout \\u00b7 0.45.1: RADIANCE RIDES THE RAIL \\u2014 the law no longer splices itself; it files a claim at station \\\"tone\\\" on ZigCore.Canon.Order's `frame.light` rail and the rail emits at one insertion point in declared station order. When AMBIENCE lands at \\\"medium\\\" it is emitted BEFORE the tone remap without a character of the radiance block changing, which is the Ambience-vs-Radiance collision settled in the contract instead of re-litigated per build. Byte-identical to 0.45.0 across the whole option matrix \\u00b7 0.45.0: RADIANCE (opts.radiance \\u2014 the FIRST Canon law: the room is a light source with no falloff, so perceived = displayed + veil and the shadow RATIOS that carry the modelling collapse in a lit space. A hue-preserving luminance remap (black-point \\u00b7 gain \\u00b7 shadow gamma \\u00b7 soft knee) spliced at the end of BOTH fragment paths; the live dial V.render6.w cross-fades it against itself. Not one character emitted when opts.radiance is absent) \\u00b7 0.44.2: two more Metal failures the LANTERN module carried \\u2014 its truncated View struct stopped at render4 while the note-flash block read render5, and its corner table was a mutable local array that overflowed Metal's small vertex stack. nvidia tolerated both \\u00b7 0.44.1:   // 0.44.1: the LANTERN module referenced BEE_SIZE, a constant declared only in the BIRD module \\u2014 nvidia tolerated the dangling reference, Metal refused it and the whole render pipeline failed to build (black canvas, healthy HUD, 60fps). Each WGSL block is a separate module and must declare what it uses \\u00b7 0.44:   // 0.44: SEPCAP (opts.sepCap \\u2014 the overlap force is unbounded and SUMS over neighbours, so a shard buried in twenty others was kicked 10-50x harder than anything else in the world; per-pair and total ceilings put it back in the same regime; byte-identical when absent) \\u00b7 0.43.4:   // 0.43.4: the BEE varying is declared for EVERY consumer (BEE or NOTEFLASH) \\u2014 the flash reads inp.bee, so turning the bee OFF while flash was ON left the fragment stage referencing an undeclared varying; bee-on worked and bee-off went black \\u00b7 0.43.3:   // 0.43.3: the BEE varying inserts ABOVE the near line, never between it and the closing brace \\u2014 MATERIAL anchors on \\\"near ... };\\\" as ONE string, so anything placed BETWEEN them destroys that anchor whichever slot it claims; verified order-independent both ways \\u00b7 0.43.2:   // 0.43.2: the BEE varying is SPLICED at @location(11) instead of written into BirdOut \\u2014 MATERIAL appends its own @location(10) snw by anchoring on the struct's last line + closing brace, so editing that text both collided on slot 10 and broke the anchor (black canvas) \\u00b7 0.43.1:   // 0.43.1: the NOTE FLASH is THE BEE ALONE (a bee varying on BirdOut gates it) \\u2014 lighting every shard's cup made the whole field answer the note and lost her in it; one lit interior among a thousand dark ones is the point \\u00b7 0.43: NOTE FLASH (opts.noteFlash \\u2014 the two faces answer a note DIFFERENTLY: the cupped INTERIOR takes the pitch-class hue while the OUTSIDE takes one fixed colour, so a turning field alternates a constant skin with an interior that differs for every pitch; render6 declared LAST in the View struct so nothing already hand-indexed shifts; byte-identical when absent) \\u00b7 0.42: THE BEE FLASHES THE NOTE (render5.z/w)/12 mapping MELODIC STRATA already uses, so a C is the same colour as a band or as her flash; her lantern is also 2.7x larger because her flash is an EVENT not one firefly among thousands; byte-identical at render5.w = 0) \\u00b7 0.41: THE BEE (agent #0 drawn at 1.45x and swelling with charisma \\u2014 the SAME letterform as every shard, only larger and self-lit, so the field turning toward her reads as recognition rather than alarm; never hidden by UNSEEN) \\u00b7 0.40: UNSEEN (opts.unseen \\u2014 a fraction of the flock fully PRESENT to the physics and not DRAWN. The crowd and the eye want opposite things: contagion and murmur need numbers, legibility needs room. Render-side splice only, so hidden agents still flock, collide, carry contagion and count as neighbours \\u2014 the shards you see are moved by neighbours you cannot; byte-identical at 0) \\u00b7 0.39: ONSET (opts.onset \\u2014 agitation may only RISE over a time constant instead of snapping to full in one frame; the max() gave contagion an instant attack and a slow release, i.e. a popcorn envelope, and agit drives vmax so a single-frame spike made individual shards DART. Brackets the contagion line rather than replacing it, since VOICE claims that text. Performer strikes stay instant; byte-identical at 0) \\u00b7 0.38: CONTACT (opts.contact {r,k,damp} \\u2014 matter that OCCUPIES SPACE, reusing the flock's own spatial grid; separation is a preference and can be overpowered, this cannot; SCATTER rewritten as GATHER so each thread keeps its own half of every pair; parity-checked against ZigCore.Contact.self by tools/parity_contact.html; byte-identical when absent) \\u00b7 0.37: STRUCTURE (opts.structure \\u2014 matter that is JOINED: spring + damping + momentum-conserving bend, SCATTER rewritten as GATHER so a compute thread only writes its own slot; chains only; parity-checked against ZigCore.Structure.accel by tools/parity_structure.html; byte-identical when absent) \\u00b7   // 0.34: INTERIOR BUFF (back-face relief \\u00d70.35 + specular broadened/dimmed + gem glints \\u00d70.5 on !ff \\u2014 the mesh facet seams stop catching as hard straight lines on the back; front keeps full crisp relief; byte-identical on the front face) \\u00b7 0.33: GEM FACE (opts.gemFace \\u2014 \\\"inside\\\" = the SEASHELL: matte material on the outside, gem nacre in the cupped interior; \\\"outside\\\"/\\\"both\\\" too; guards the gem's c=gc by front_facing; byte-identical at \\\"both\\\") \\u00b7 0.32: MELODIC STRATA (View.noteBands[6] \\u00b7 view[84..107] \\u2014 each EWI note blooms a band of light at its pitch-height in its pitch-class colour, fading over time; the melody written onto the body's vertical axis; driven by ZC.NoteField; zero when silent) \\u00b7 0.31: SILHOUETTE RIM (live V.render5.x/y \\u2014 a fresnel edge re-draws every letter's outline against the void, legible under any material on either face; face-corrected so the concave back outlines too; zero at render5.x=0; the reusable legibility capability all species inherit) \\u00b7 0.30: GEM MATERIALS (opts.gem \\u2014 refraction + dispersion fire + fresnel sky-reflection + facet flash + sparkle, sampling the analytic sky; byte-identical off) \\u00b7 0.29: FABRIC UNDERSIDE (opts.backFabric \\u2014 20 textiles: weave pattern + sheen model + colour lining the concave back; byte-identical off) \\u00b7 0.28: VELVET UNDERSIDE (opts.backVelvet \\u2014 a different fabric skin on the back face: deep matte + grazing sheen; byte-identical off) \\u00b7 0.27: MEMORY UNDERSIDE (opts.memoryBack \\u2014 the back face glows with a lagging ghost of the recent phrase; the 2nd performance surface; render4.y/z; byte-identical off) \\u00b7 0.26: CHIAROSCURO (opts.chiaro \\u2014 back off ambient/fill so only the light-facing side shows, unlit \\u2192 black; byte-identical at 0) \\u00b7 0.25: WEB \\u2014 connective filaments between neighbouring agents (grid-driven K-NN compute + instanced thread render; breath strings the web; byte-identical when opts.web absent) \\u00b7 0.24: GRAIN THROUGH COLOUR \\u2014 the skin's grain corrugates the spectrum too, so surface texture survives at full ink (byte-identical without material/spectrum) \\u00b7 0.22: BOUNDARY \\u00b7 0.23: COMPOSE \\u2014 spectrum TINTS the material body (visible on pale skins; ink I/K = solid\\u2194rainbow amount over the pigment) \\u00b7 0.19: MEDIUM \\u00b7 0.20: FORCES \\u00b7 0.21: CURRENT (opts.boundary {shape,r,k,lo?,hi?} \\u2014 the world's SHAPE: a soft cylinder/sphere that holds matter inside a volume; restoring accel before integrate, composes with all; byte-identical when absent) // 0.35: BOUNDARY AXIS \\u2014 cylinder law holds along any free axis (capsule = horizontal cigar); byte-identical for axis \\\"y\\\" \\u00b7 0.35.1: GYRE AXIS \\u2014 current circulates around any axis (roll a horizontal cigar broadside); byte-identical for gyre axis \\\"y\\\" \\u00b7 0.36: ELLIPSOID boundary (lens \\u2014 squashed sphere, per-axis radii; the wide breathing disc); byte-identical for sphere/cylinder\\n\", \"new\": \"  const ZigWebGPU = global.ZigWebGPU || (global.ZigWebGPU = {});\\n  ZigWebGPU.VERSION = \\\"0.47.0\\\";   // 0.47.0: DUAL GEM (opts.gem2 \\u2014 a SECOND stone lining the cupped INTERIOR while opts.gem keeps the shell, so one shard carries two different optical behaviours: the stones differ in dispersion, facet and spark as well as colour, so diamond outside + emerald inside is hard spectral fire on the shell and a soft included glow within. gemBlock was ALREADY parameterised by stone and by face condition, so this is a second invocation at the same splice point \\u2014 no new bindings, no new varyings, nothing that can change an auto-derived layout, which is the 0.46.0 fault it cannot repeat. Both stones share Q (V.render3.x) held apart by a CONSTANT hue offset (opts.gem2Hue), so the pair sweeps together and the interval holds at every position. v0.34's interior buff becomes opts.gemBuff, defaulted to its old 0.5 \\u2014 an interior stone CHOSEN deserves to be seen. THE TRAP CLOSED: gemHueRot/gemSky are declared on GEM || GEM2, because gem2 alone would otherwise reference undeclared functions \\u2014 nvidia tolerates that, Metal refuses it, which is 0.44.1 exactly. gemFace is moot once a second stone is named, so the shell takes ff and the panel greys the control. Byte-identical when gem2 is absent) \\u00b7 0.46.0: GROUND IS WIRED \\u2014 a declared ground now sets the sky triple, the three scene clear values (NOT the trail's two, where black means zero), the afterimage's compositing/decay/gate, and the Radiance room, from one word. Signed compositing is SPLICED, so `void` and `dusk` emit the base afterimage byte for byte. THE FAULT THAT COST 8/18: the base blitFs never used the uniform, so `layout:\\\"auto\\\"` gave it a 2-entry layout; the signed blitFs uses A.lift, the layout became 3, and every bind group was rejected \\u2014 1,240 driver errors per run at 165fps with a black screen. A shader splice that changes WHICH BINDINGS A STAGE USES changes its auto-derived layout \\u00b7 0.45.1: RADIANCE RIDES THE RAIL \\u2014 the law no longer splices itself; it files a claim at station \\\"tone\\\" on ZigCore.Canon.Order's `frame.light` rail and the rail emits at one insertion point in declared station order. When AMBIENCE lands at \\\"medium\\\" it is emitted BEFORE the tone remap without a character of the radiance block changing, which is the Ambience-vs-Radiance collision settled in the contract instead of re-litigated per build. Byte-identical to 0.45.0 across the whole option matrix \\u00b7 0.45.0: RADIANCE (opts.radiance \\u2014 the FIRST Canon law: the room is a light source with no falloff, so perceived = displayed + veil and the shadow RATIOS that carry the modelling collapse in a lit space. A hue-preserving luminance remap (black-point \\u00b7 gain \\u00b7 shadow gamma \\u00b7 soft knee) spliced at the end of BOTH fragment paths; the live dial V.render6.w cross-fades it against itself. Not one character emitted when opts.radiance is absent) \\u00b7 0.44.2: two more Metal failures the LANTERN module carried \\u2014 its truncated View struct stopped at render4 while the note-flash block read render5, and its corner table was a mutable local array that overflowed Metal's small vertex stack. nvidia tolerated both \\u00b7 0.44.1:   // 0.44.1: the LANTERN module referenced BEE_SIZE, a constant declared only in the BIRD module \\u2014 nvidia tolerated the dangling reference, Metal refused it and the whole render pipeline failed to build (black canvas, healthy HUD, 60fps). Each WGSL block is a separate module and must declare what it uses \\u00b7 0.44:   // 0.44: SEPCAP (opts.sepCap \\u2014 the overlap force is unbounded and SUMS over neighbours, so a shard buried in twenty others was kicked 10-50x harder than anything else in the world; per-pair and total ceilings put it back in the same regime; byte-identical when absent) \\u00b7 0.43.4:   // 0.43.4: the BEE varying is declared for EVERY consumer (BEE or NOTEFLASH) \\u2014 the flash reads inp.bee, so turning the bee OFF while flash was ON left the fragment stage referencing an undeclared varying; bee-on worked and bee-off went black \\u00b7 0.43.3:   // 0.43.3: the BEE varying inserts ABOVE the near line, never between it and the closing brace \\u2014 MATERIAL anchors on \\\"near ... };\\\" as ONE string, so anything placed BETWEEN them destroys that anchor whichever slot it claims; verified order-independent both ways \\u00b7 0.43.2:   // 0.43.2: the BEE varying is SPLICED at @location(11) instead of written into BirdOut \\u2014 MATERIAL appends its own @location(10) snw by anchoring on the struct's last line + closing brace, so editing that text both collided on slot 10 and broke the anchor (black canvas) \\u00b7 0.43.1:   // 0.43.1: the NOTE FLASH is THE BEE ALONE (a bee varying on BirdOut gates it) \\u2014 lighting every shard's cup made the whole field answer the note and lost her in it; one lit interior among a thousand dark ones is the point \\u00b7 0.43: NOTE FLASH (opts.noteFlash \\u2014 the two faces answer a note DIFFERENTLY: the cupped INTERIOR takes the pitch-class hue while the OUTSIDE takes one fixed colour, so a turning field alternates a constant skin with an interior that differs for every pitch; render6 declared LAST in the View struct so nothing already hand-indexed shifts; byte-identical when absent) \\u00b7 0.42: THE BEE FLASHES THE NOTE (render5.z/w)/12 mapping MELODIC STRATA already uses, so a C is the same colour as a band or as her flash; her lantern is also 2.7x larger because her flash is an EVENT not one firefly among thousands; byte-identical at render5.w = 0) \\u00b7 0.41: THE BEE (agent #0 drawn at 1.45x and swelling with charisma \\u2014 the SAME letterform as every shard, only larger and self-lit, so the field turning toward her reads as recognition rather than alarm; never hidden by UNSEEN) \\u00b7 0.40: UNSEEN (opts.unseen \\u2014 a fraction of the flock fully PRESENT to the physics and not DRAWN. The crowd and the eye want opposite things: contagion and murmur need numbers, legibility needs room. Render-side splice only, so hidden agents still flock, collide, carry contagion and count as neighbours \\u2014 the shards you see are moved by neighbours you cannot; byte-identical at 0) \\u00b7 0.39: ONSET (opts.onset \\u2014 agitation may only RISE over a time constant instead of snapping to full in one frame; the max() gave contagion an instant attack and a slow release, i.e. a popcorn envelope, and agit drives vmax so a single-frame spike made individual shards DART. Brackets the contagion line rather than replacing it, since VOICE claims that text. Performer strikes stay instant; byte-identical at 0) \\u00b7 0.38: CONTACT (opts.contact {r,k,damp} \\u2014 matter that OCCUPIES SPACE, reusing the flock's own spatial grid; separation is a preference and can be overpowered, this cannot; SCATTER rewritten as GATHER so each thread keeps its own half of every pair; parity-checked against ZigCore.Contact.self by tools/parity_contact.html; byte-identical when absent) \\u00b7 0.37: STRUCTURE (opts.structure \\u2014 matter that is JOINED: spring + damping + momentum-conserving bend, SCATTER rewritten as GATHER so a compute thread only writes its own slot; chains only; parity-checked against ZigCore.Structure.accel by tools/parity_structure.html; byte-identical when absent) \\u00b7   // 0.34: INTERIOR BUFF (back-face relief \\u00d70.35 + specular broadened/dimmed + gem glints \\u00d70.5 on !ff \\u2014 the mesh facet seams stop catching as hard straight lines on the back; front keeps full crisp relief; byte-identical on the front face) \\u00b7 0.33: GEM FACE (opts.gemFace \\u2014 \\\"inside\\\" = the SEASHELL: matte material on the outside, gem nacre in the cupped interior; \\\"outside\\\"/\\\"both\\\" too; guards the gem's c=gc by front_facing; byte-identical at \\\"both\\\") \\u00b7 0.32: MELODIC STRATA (View.noteBands[6] \\u00b7 view[84..107] \\u2014 each EWI note blooms a band of light at its pitch-height in its pitch-class colour, fading over time; the melody written onto the body's vertical axis; driven by ZC.NoteField; zero when silent) \\u00b7 0.31: SILHOUETTE RIM (live V.render5.x/y \\u2014 a fresnel edge re-draws every letter's outline against the void, legible under any material on either face; face-corrected so the concave back outlines too; zero at render5.x=0; the reusable legibility capability all species inherit) \\u00b7 0.30: GEM MATERIALS (opts.gem \\u2014 refraction + dispersion fire + fresnel sky-reflection + facet flash + sparkle, sampling the analytic sky; byte-identical off) \\u00b7 0.29: FABRIC UNDERSIDE (opts.backFabric \\u2014 20 textiles: weave pattern + sheen model + colour lining the concave back; byte-identical off) \\u00b7 0.28: VELVET UNDERSIDE (opts.backVelvet \\u2014 a different fabric skin on the back face: deep matte + grazing sheen; byte-identical off) \\u00b7 0.27: MEMORY UNDERSIDE (opts.memoryBack \\u2014 the back face glows with a lagging ghost of the recent phrase; the 2nd performance surface; render4.y/z; byte-identical off) \\u00b7 0.26: CHIAROSCURO (opts.chiaro \\u2014 back off ambient/fill so only the light-facing side shows, unlit \\u2192 black; byte-identical at 0) \\u00b7 0.25: WEB \\u2014 connective filaments between neighbouring agents (grid-driven K-NN compute + instanced thread render; breath strings the web; byte-identical when opts.web absent) \\u00b7 0.24: GRAIN THROUGH COLOUR \\u2014 the skin's grain corrugates the spectrum too, so surface texture survives at full ink (byte-identical without material/spectrum) \\u00b7 0.22: BOUNDARY \\u00b7 0.23: COMPOSE \\u2014 spectrum TINTS the material body (visible on pale skins; ink I/K = solid\\u2194rainbow amount over the pigment) \\u00b7 0.19: MEDIUM \\u00b7 0.20: FORCES \\u00b7 0.21: CURRENT (opts.boundary {shape,r,k,lo?,hi?} \\u2014 the world's SHAPE: a soft cylinder/sphere that holds matter inside a volume; restoring accel before integrate, composes with all; byte-identical when absent) // 0.35: BOUNDARY AXIS \\u2014 cylinder law holds along any free axis (capsule = horizontal cigar); byte-identical for axis \\\"y\\\" \\u00b7 0.35.1: GYRE AXIS \\u2014 current circulates around any axis (roll a horizontal cigar broadside); byte-identical for gyre axis \\\"y\\\" \\u00b7 0.36: ELLIPSOID boundary (lens \\u2014 squashed sphere, per-axis radii; the wide breathing disc); byte-identical for sphere/cylinder\\n\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"    const RAD = opts.radiance || null;\\n    const GEMFACE = (opts.gemFace || \\\"both\\\");\", \"new\": \"    const RAD = opts.radiance || null;\\n    const GEM2 = opts.gem2 || null;   // DUAL GEM (v0.47): a SECOND stone lining the cupped INTERIOR while opts.gem keeps the shell \\u2014 two different optical behaviours on two faces of one shard. Byte-identical when absent.\\n    const GEM2HUE = +opts.gem2Hue || 0;   // a CONSTANT hue offset (0..1 wheel) for the interior stone: Q sweeps the PAIR and the interval between them holds at every position\\n    const GEMBUFF = (opts.gemBuff === undefined || opts.gemBuff === null) ? 0.5 : +opts.gemBuff;   // v0.34's interior glint softening, now a parameter \\u2014 an interior stone CHOSEN deserves to be seen (1.0 = no softening)\\n    const GEMFACE = (opts.gemFace || \\\"both\\\");\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"    const GEMFACE = (opts.gemFace || \\\"both\\\");\\n    const GEMCOND = GEMFACE === \\\"inside\\\" ? \\\"if (!ff) \\\" : (GEMFACE === \\\"outside\\\" ? \\\"if (ff) \\\" : \\\"\\\");\\n    const gemBlock = (G, cond) => {\\n      const f = (x) => { const s = (Math.round((+x) * 1e5) / 1e5).toString(); return s.indexOf(\\\".\\\") < 0 && s.indexOf(\\\"e\\\") < 0 ? s + \\\".0\\\" : s; };\", \"new\": \"    const GEMFACE = (opts.gemFace || \\\"both\\\");\\n    /* a second stone speaks for the interior, so gemFace has nothing left to decide: the shell stone takes ff */\\n    const GEMCOND = GEM2 ? \\\"if (ff) \\\" : (GEMFACE === \\\"inside\\\" ? \\\"if (!ff) \\\" : (GEMFACE === \\\"outside\\\" ? \\\"if (ff) \\\" : \\\"\\\"));\\n    const gemBlock = (G, cond, hueOff) => {\\n      const f = (x) => { const s = (Math.round((+x) * 1e5) / 1e5).toString(); return s.indexOf(\\\".\\\") < 0 && s.indexOf(\\\"e\\\") < 0 ? s + \\\".0\\\" : s; };\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"      const f = (x) => { const s = (Math.round((+x) * 1e5) / 1e5).toString(); return s.indexOf(\\\".\\\") < 0 && s.indexOf(\\\"e\\\") < 0 ? s + \\\".0\\\" : s; };\\n      const eta = 1 / (+G.ior || 1.6), d = +G.disp || 0.015;\\n      return `${cond || \\\"\\\"}{\", \"new\": \"      const f = (x) => { const s = (Math.round((+x) * 1e5) / 1e5).toString(); return s.indexOf(\\\".\\\") < 0 && s.indexOf(\\\"e\\\") < 0 ? s + \\\".0\\\" : s; };\\n      const eta = 1 / (+G.ior || 1.6), d = +G.disp || 0.015, HOFF = +hueOff || 0;\\n      return `${cond || \\\"\\\"}{\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"      var gN = Nr; if (!ff) { gN = -gN; }\\n      let gBuff = select(1.0, 0.5, !ff);   // BUFF: soften the INTERIOR gem's sharp glints (nacre reads soft, not a hard cut) so it doesn't redraw facet lines on the back\\n      let gToC = normalize(V.camPos.xyz - inp.wpos);\", \"new\": \"      var gN = Nr; if (!ff) { gN = -gN; }\\n      let gBuff = select(1.0, ${f(GEMBUFF)}, !ff);   // BUFF: soften the INTERIOR gem's sharp glints (nacre reads soft, not a hard cut) so it doesn't redraw facet lines on the back\\n      let gToC = normalize(V.camPos.xyz - inp.wpos);\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"      let fire = vec3f(gemSky(rR).r, gemSky(rG).g, gemSky(rB).b);\\n      let gemC = gemHueRot(vec3f(${f(G.col[0])}, ${f(G.col[1])}, ${f(G.col[2])}), V.render3.x * 6.2831);   // Q rotates the stone's hue live\\n      let core = fire * gemC * 2.0 + gemC * (0.22 + 0.6 * fres);   // brighter refracted fire + a luminous colour core, so the stone reads even in a dark room\", \"new\": \"      let fire = vec3f(gemSky(rR).r, gemSky(rG).g, gemSky(rB).b);\\n      let gemC = gemHueRot(vec3f(${f(G.col[0])}, ${f(G.col[1])}, ${f(G.col[2])}), ${HOFF ? `V.render3.x * 6.2831 + ${f(HOFF * 6.2831)}` : `V.render3.x * 6.2831`});   // Q rotates the stone's hue live\\n      let core = fire * gemC * 2.0 + gemC * (0.22 + 0.6 * fres);   // brighter refracted fire + a luminous colour core, so the stone reads even in a dark room\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"    }` : ``}\\n    ${(GEM && MAT) ? gemBlock(GEM, GEMCOND) : ``}\\n    c *= 0.82 + 0.18 * (1.0 - abs(inp.dv));                 // edge occlusion\", \"new\": \"    }` : ``}\\n    ${(GEM && MAT) ? gemBlock(GEM, GEMCOND) : ``}${(GEM2 && MAT) ? \\\"\\\\n    \\\" + gemBlock(GEM2, \\\"if (!ff) \\\", GEM2HUE) : ``}\\n    c *= 0.82 + 0.18 * (1.0 - abs(inp.dv));                 // edge occlusion\"}, {\"file\": \"engine/zigwebgpu.js\", \"old\": \"         after V's binding so it can read the View uniform. */\\n      if (GEM) RENDER_SRC = RENDER_SRC.replace(\\n        \\\"@fragment\\\\nfn birdFs(inp: BirdOut, @builtin(front_facing) ff: bool) -> @location(0) vec4f {\\\",\", \"new\": \"         after V's binding so it can read the View uniform. */\\n      if (GEM || GEM2) RENDER_SRC = RENDER_SRC.replace(\\n        \\\"@fragment\\\\nfn birdFs(inp: BirdOut, @builtin(front_facing) ff: bool) -> @location(0) vec4f {\\\",\"}, {\"file\": \"species/sickleswarm.js\", \"old\": \"    }\\n    /* FABRIC UNDERSIDE \\u2014 a textile lining the concave interior (ZigCore.Fabrics: velvet, silk,\", \"new\": \"    }\\n    /* DUAL GEM \\u2014 a SECOND stone lining the cupped INTERIOR while ZIG_GEM keeps the shell.\\n       The stones differ in more than colour: dispersion, facet and spark are per-stone, so\\n       diamond outside + emerald inside is hard spectral fire on the shell and a soft included\\n       glow within \\u2014 two optical behaviours on two faces of one shard.\\n       ZIG_GEM2 = gem name \\u00b7 ZIG_GEM2HUE overrides its body colour (0..1 wheel)\\n       ZIG_GEM2OFFSET = constant hue interval held between the two stones as Q sweeps the pair\\n       ZIG_GEMBUFF = interior glint softening (default 0.5 = v0.34's buff \\u00b7 1.0 = none) */\\n    let GEM2PROF = null;\\n    {\\n      const G = (ZC.Gems || {})[(global.ZIG_GEM2 || \\\"\\\").toLowerCase()];\\n      if (G) {\\n        let col = G.col.slice();\\n        if (global.ZIG_GEM2HUE !== undefined) {\\n          const gh = +global.ZIG_GEM2HUE, hc = (p) => 0.5 + 0.5 * Math.cos(6.2831 * (gh + p));\\n          const hr = hc(0), hg = hc(0.3333), hb = hc(0.6667), mn = Math.min(hr, hg, hb);\\n          col = [(hr - mn) * 0.85 + 0.1, (hg - mn) * 0.85 + 0.1, (hb - mn) * 0.85 + 0.1];\\n        }\\n        GEM2PROF = { col, ior: G.ior, disp: G.disp, facet: G.facet, spark: G.spark };\\n      }\\n    }\\n    /* FABRIC UNDERSIDE \\u2014 a textile lining the concave interior (ZigCore.Fabrics: velvet, silk,\"}, {\"file\": \"species/sickleswarm.js\", \"old\": \"      gem: GEMPROF,                                         // \\u2190 GEM MATERIAL: the shard becomes a cut stone (refraction/dispersion/fresnel/facet), sampling the sky\\n      gemFace: ([\\\"both\\\", \\\"inside\\\", \\\"outside\\\"].indexOf(String(global.ZIG_GEMFACE || \\\"both\\\")) >= 0 ? String(global.ZIG_GEMFACE || \\\"both\\\") : \\\"both\\\")   // SEASHELL: \\\"inside\\\" = gem lines the cupped interior, material stays the outside\\n    });   // CHIAROSCURO is a LIVE uniform now (view[76]), driven by dial.chiaro + keys 1/2\", \"new\": \"      gem: GEMPROF,                                         // \\u2190 GEM MATERIAL: the shard becomes a cut stone (refraction/dispersion/fresnel/facet), sampling the sky\\n      gemFace: ([\\\"both\\\", \\\"inside\\\", \\\"outside\\\"].indexOf(String(global.ZIG_GEMFACE || \\\"both\\\")) >= 0 ? String(global.ZIG_GEMFACE || \\\"both\\\") : \\\"both\\\"),   // SEASHELL: \\\"inside\\\" = gem lines the cupped interior, material stays the outside\\n      gem2: GEM2PROF,                                       // \\u2190 DUAL GEM: the interior's own stone (gemFace is moot once this is set \\u2014 the shell takes the outside)\\n      gem2Hue: (global.ZIG_GEM2OFFSET !== undefined ? +global.ZIG_GEM2OFFSET : undefined),   // \\u2190 the interval the pair holds while Q sweeps them together\\n      gemBuff: (global.ZIG_GEMBUFF !== undefined ? +global.ZIG_GEMBUFF : undefined)          // \\u2190 interior glint softening (absent = 0.5, v0.34's value)\\n    });   // CHIAROSCURO is a LIVE uniform now (view[76]), driven by dial.chiaro + keys 1/2\"}, {\"file\": \"zigverse_engine.html\", \"old\": \"  <div class=\\\"gem\\\"><label>GEM</label> <select id=\\\"gempick\\\" title=\\\"none, or a cut stone (refraction \\u00b7 dispersion \\u00b7 facet flash) \\u2014 reloads\\\"></select></div>\\n  <div class=\\\"gem\\\"><label>GEM FACE</label> <select id=\\\"gemfacepick\\\" title=\\\"where the gem lives \\u2014 inside (seashell nacre) \\u00b7 outside (gem shell) \\u00b7 both (whole shard) \\u2014 reloads\\\"></select></div>\", \"new\": \"  <div class=\\\"gem\\\"><label>GEM</label> <select id=\\\"gempick\\\" title=\\\"none, or a cut stone (refraction \\u00b7 dispersion \\u00b7 facet flash) \\u2014 reloads\\\"></select></div>\\n  <div class=\\\"gem\\\"><label>GEM 2</label> <select id=\\\"gem2pick\\\" title=\\\"a SECOND stone lining the cupped interior while GEM keeps the shell \\u2014 two optical behaviours on one shard; overrides GEM FACE \\u2014 reloads\\\"></select></div>\\n  <div class=\\\"gem\\\"><label>GEM FACE</label> <select id=\\\"gemfacepick\\\" title=\\\"where the gem lives \\u2014 inside (seashell nacre) \\u00b7 outside (gem shell) \\u00b7 both (whole shard) \\u2014 reloads\\\"></select></div>\"}, {\"file\": \"zigverse_engine.html\", \"old\": \"  window.ZIG_BACKMAT = (function () { var f = (location.hash.match(/fab=([a-z]+)/i) || [])[1]; return (f && f !== \\\"none\\\") ? f : undefined; })();\\n  window.ZIG_GEMFACE = ((location.hash.match(/gface=([a-z]+)/i) || [])[1]) || \\\"both\\\";   // SEASHELL: inside = gem lines the cupped interior \\u00b7 outside = gem shell \\u00b7 both = whole shard\", \"new\": \"  window.ZIG_BACKMAT = (function () { var f = (location.hash.match(/fab=([a-z]+)/i) || [])[1]; return (f && f !== \\\"none\\\") ? f : undefined; })();\\n  window.ZIG_GEM2    = (function () { var g = (location.hash.match(/gem2=([a-z]+)/i) || [])[1]; return (g && g !== \\\"none\\\") ? g : undefined; })();   // DUAL GEM: the interior's own stone\\n  window.ZIG_GEM2OFFSET = (function () { var v = (location.hash.match(/gem2off=([0-9.]+)/i) || [])[1]; return v === undefined ? undefined : +v; })();   // the interval the pair holds while Q sweeps\\n  window.ZIG_GEMBUFF = (function () { var v = (location.hash.match(/gembuff=([0-9.]+)/i) || [])[1]; return v === undefined ? undefined : +v; })();      // interior glint softening (absent = 0.5)\\n  window.ZIG_GEMFACE = ((location.hash.match(/gface=([a-z]+)/i) || [])[1]) || \\\"both\\\";   // SEASHELL: inside = gem lines the cupped interior \\u00b7 outside = gem shell \\u00b7 both = whole shard\"}, {\"file\": \"zigverse_engine.html\", \"old\": \"    const gsel = document.getElementById(\\\"gempick\\\"), fbsel = document.getElementById(\\\"fabpick\\\"), gfsel = document.getElementById(\\\"gemfacepick\\\");\\n    const fill = (sel, names, cur) => { if (!sel) return; for (const n of names) { const o = document.createElement(\\\"option\\\"); o.value = n; o.textContent = n; if (n === cur) o.selected = true; sel.appendChild(o); } };\", \"new\": \"    const gsel = document.getElementById(\\\"gempick\\\"), fbsel = document.getElementById(\\\"fabpick\\\"), gfsel = document.getElementById(\\\"gemfacepick\\\");\\n    const g2sel = document.getElementById(\\\"gem2pick\\\");\\n    const fill = (sel, names, cur) => { if (!sel) return; for (const n of names) { const o = document.createElement(\\\"option\\\"); o.value = n; o.textContent = n; if (n === cur) o.selected = true; sel.appendChild(o); } };\"}, {\"file\": \"zigverse_engine.html\", \"old\": \"    const fill = (sel, names, cur) => { if (!sel) return; for (const n of names) { const o = document.createElement(\\\"option\\\"); o.value = n; o.textContent = n; if (n === cur) o.selected = true; sel.appendChild(o); } };\\n    const skinTail = () => \\\"&gem=\\\" + (gsel ? gsel.value : \\\"none\\\") + \\\"&fab=\\\" + (fbsel ? fbsel.value : \\\"none\\\") + \\\"&gface=\\\" + (gfsel ? gfsel.value : \\\"both\\\");   // GEM \\u00b7 GEM FACE \\u00b7 FABRIC persist across every reload\\n    const bondTail = () => \\\"&bond=\\\" + (bdsel ? bdsel.value : \\\"none\\\") + \\\"&spread=\\\" + (spsel ? spsel.value : \\\"field\\\") + \\\"&still=\\\" + (stsel ? stsel.value : \\\"restless\\\") + \\\"&contact=\\\" + (ctsel ? ctsel.value : \\\"off\\\") + \\\"&onset=\\\" + (onsel ? onsel.value : \\\"snap\\\") + \\\"&crowding=\\\" + (cwsel ? cwsel.value : \\\"loose\\\") + \\\"&summon=\\\" + (susel ? susel.value : \\\"note\\\") + \\\"&heft=\\\" + (hfsel ? hfsel.value : \\\"cupped\\\") + \\\"&crowd=\\\" + (cnsel ? cnsel.value : \\\"6000\\\") + \\\"&unseen=\\\" + (unsel ? unsel.value : \\\"all\\\") + \\\"&bee=\\\" + (besel ? besel.value : \\\"off\\\") + \\\"&flash=\\\" + (nfsel ? nfsel.value : \\\"off\\\");   // BOND rides along in either mode\", \"new\": \"    const fill = (sel, names, cur) => { if (!sel) return; for (const n of names) { const o = document.createElement(\\\"option\\\"); o.value = n; o.textContent = n; if (n === cur) o.selected = true; sel.appendChild(o); } };\\n    const skinTail = () => \\\"&gem=\\\" + (gsel ? gsel.value : \\\"none\\\") + \\\"&gem2=\\\" + (g2sel ? g2sel.value : \\\"none\\\") + \\\"&fab=\\\" + (fbsel ? fbsel.value : \\\"none\\\") + \\\"&gface=\\\" + (gfsel ? gfsel.value : \\\"both\\\");   // GEM \\u00b7 GEM FACE \\u00b7 FABRIC persist across every reload\\n    const bondTail = () => \\\"&bond=\\\" + (bdsel ? bdsel.value : \\\"none\\\") + \\\"&spread=\\\" + (spsel ? spsel.value : \\\"field\\\") + \\\"&still=\\\" + (stsel ? stsel.value : \\\"restless\\\") + \\\"&contact=\\\" + (ctsel ? ctsel.value : \\\"off\\\") + \\\"&onset=\\\" + (onsel ? onsel.value : \\\"snap\\\") + \\\"&crowding=\\\" + (cwsel ? cwsel.value : \\\"loose\\\") + \\\"&summon=\\\" + (susel ? susel.value : \\\"note\\\") + \\\"&heft=\\\" + (hfsel ? hfsel.value : \\\"cupped\\\") + \\\"&crowd=\\\" + (cnsel ? cnsel.value : \\\"6000\\\") + \\\"&unseen=\\\" + (unsel ? unsel.value : \\\"all\\\") + \\\"&bee=\\\" + (besel ? besel.value : \\\"off\\\") + \\\"&flash=\\\" + (nfsel ? nfsel.value : \\\"off\\\");   // BOND rides along in either mode\"}, {\"file\": \"zigverse_engine.html\", \"old\": \"    fill(fbsel, [\\\"none\\\"].concat(ZigCore.Fabrics ? Object.keys(ZigCore.Fabrics) : []), (location.hash.match(/fab=([a-z]+)/i) || [])[1] || \\\"none\\\");\\n    fill(gfsel, [\\\"both\\\", \\\"inside\\\", \\\"outside\\\"], window.ZIG_GEMFACE);\", \"new\": \"    fill(fbsel, [\\\"none\\\"].concat(ZigCore.Fabrics ? Object.keys(ZigCore.Fabrics) : []), (location.hash.match(/fab=([a-z]+)/i) || [])[1] || \\\"none\\\");\\n    fill(g2sel, [\\\"none\\\"].concat(ZigCore.Gems ? Object.keys(ZigCore.Gems) : []), (location.hash.match(/gem2=([a-z]+)/i) || [])[1] || \\\"none\\\");\\n    fill(gfsel, [\\\"both\\\", \\\"inside\\\", \\\"outside\\\"], window.ZIG_GEMFACE);\"}, {\"file\": \"zigverse_engine.html\", \"old\": \"    fill(gfsel, [\\\"both\\\", \\\"inside\\\", \\\"outside\\\"], window.ZIG_GEMFACE);\\n    /* When a named world is active, reflect its triple + native (or overridden) skin & shape\", \"new\": \"    fill(gfsel, [\\\"both\\\", \\\"inside\\\", \\\"outside\\\"], window.ZIG_GEMFACE);\\n    /* a second stone speaks for the interior, so GEM FACE has nothing left to decide.\\n       GREYED rather than removed: a control that silently stops mattering is how an\\n       afternoon gets spent chasing a dial that does nothing. */\\n    const gfSync = () => { const on = !!(g2sel && g2sel.value !== \\\"none\\\"); if (gfsel) { gfsel.disabled = on; gfsel.style.opacity = on ? 0.35 : 1; gfsel.title = on ? \\\"GEM 2 is set \\u2014 both faces are spoken for\\\" : \\\"where the gem lives \\u2014 inside (seashell nacre) \\u00b7 outside (gem shell) \\u00b7 both (whole shard) \\u2014 reloads\\\"; } };\\n    if (g2sel) g2sel.addEventListener(\\\"change\\\", gfSync);\\n    gfSync();\\n    /* When a named world is active, reflect its triple + native (or overridden) skin & shape\"}]");
const GATE = JSON.parse("\"/* =============================================================================\\n   test/gem2_ref.mjs \\u2014 DUAL GEM (v0.47), PROVED ON THE CPU\\n   run: node test/gem2_ref.mjs\\n\\n   opts.gem2 puts a SECOND stone on the cupped interior while opts.gem keeps the\\n   shell. gemBlock was already parameterised by stone AND face condition, so the\\n   whole law is a second invocation at the same splice point \\u2014 no new bindings,\\n   no new varyings, nothing that can change an auto-derived layout.\\n\\n   Case C is why this file exists. The gem helper declarations (gemHueRot,\\n   gemSky) were gated on GEM alone. A second stone with no first would have\\n   emitted a block calling undeclared functions \\u2014 nvidia tolerates a dangling\\n   reference, Metal refuses it and the whole pipeline fails to build: a black\\n   canvas with a healthy HUD at 165fps. That is 0.44.1 exactly, and it would\\n   have shipped invisibly on eyeZ.\\n\\n   Byte-identity when gem2 is absent is tools/byte_identity.mjs's job, not this\\n   one. This proves the law is PRESENT and correct when it is declared.\\n   ========================================================================== */\\nimport { readFileSync } from \\\"fs\\\"; import vm from \\\"node:vm\\\";\\nimport path from \\\"node:path\\\"; import { fileURLToPath } from \\\"node:url\\\";\\nconst ROOT = path.dirname(path.dirname(fileURLToPath(import.meta.url)));\\nconst STUB=`globalThis.__shaders=[];globalThis.__err='';\\nglobalThis.GPUBufferUsage={UNIFORM:64,COPY_DST:8,STORAGE:128,COPY_SRC:4,VERTEX:32,INDEX:16,INDIRECT:256,MAP_READ:1};\\nglobalThis.GPUTextureUsage={RENDER_ATTACHMENT:16,TEXTURE_BINDING:4,COPY_DST:2,COPY_SRC:1,STORAGE_BINDING:8};\\nglobalThis.GPUShaderStage={VERTEX:1,FRAGMENT:2,COMPUTE:4};globalThis.GPUMapMode={READ:1,WRITE:2};\\nglobalThis.__mkGpu=function(){const nul=()=>({destroy(){},createView:()=>({})});const pipe=()=>({getBindGroupLayout:()=>({})});\\nreturn{device:{createBuffer:nul,createTexture:nul,createSampler:()=>({}),createShaderModule:(d)=>{globalThis.__shaders.push(d.code);return{}},\\ncreateBindGroupLayout:()=>({}),createPipelineLayout:()=>({}),createComputePipeline:pipe,createRenderPipeline:pipe,\\ncreateBindGroup:()=>({}),createCommandEncoder:()=>({}),queue:{writeBuffer(){},submit(){},writeTexture(){}},\\nlimits:{},features:{has:()=>false},destroy(){}},format:\\\"bgra8unorm\\\",canvas:{width:1920,height:1080},ctx:{},configure(){},aspect:16/9}};`;\\nfunction run(extra){\\n  const ctx=vm.createContext({console,Math,JSON,Float32Array,Uint32Array,Int32Array,Uint8Array,ArrayBuffer,Object,Array,String,Number,Boolean,Error,TypeError,Map,Set,isNaN,parseInt,parseFloat,performance,Date});\\n  vm.runInContext(STUB,ctx);\\n  for(const f of[\\\"zigcore.js\\\",\\\"zigmesh.js\\\",\\\"zigwebgpu.js\\\"]) vm.runInContext(readFileSync(ROOT+\\\"/engine/\\\"+f,\\\"utf8\\\"),ctx,{filename:f});\\n  vm.runInContext(`const OPTS={max:20000,count:6000,seed:1234,extent:60,extentY:30,cell:12,debris:0,mesh:ZigMesh.make(ZigMesh.presets.sicklePetal,{refine:1})};\\n  Object.assign(OPTS,${JSON.stringify(extra||{})});\\n  try{ZigWebGPU.createFlock(__mkGpu(),OPTS)}catch(e){globalThis.__err=String(e).slice(0,140)}`,ctx);\\n  return{src:vm.runInContext(\\\"__shaders\\\",ctx).join(\\\"\\\\n\\\"),err:vm.runInContext(\\\"__err||''\\\",ctx)};\\n}\\nconst NACRE={dark:[0.12,0.115,0.13],light:[0.92,0.90,0.88],moon:[0.55,0.58,0.72],iriBase:0.55,iriBurst:2.9,tex:[2,18,0.16,26,0.5,0.15,0.65,0]};\\nconst DIAMOND={col:[0.93,0.96,1.00],ior:2.42,disp:0.045,facet:1.00,spark:1.00};\\nconst EMERALD={col:[0.05,0.70,0.50],ior:1.58,disp:0.014,facet:0.52,spark:0.42};\\nconst T=(n,c)=>console.log((c?\\\"  OK   \\\":\\\"  FAIL \\\")+n);\\nlet bad=0; const chk=(n,c)=>{T(n,c); if(!c)bad++;};\\n\\nconsole.log(\\\"A \\u2014 gem alone (shell only)\\\");\\nlet a=run({material:NACRE,gem:DIAMOND});\\nchk(\\\"no error\\\", !a.err); if(a.err)console.log(\\\"     \\\"+a.err);\\nchk(\\\"one gem block\\\", (a.src.match(/Q rotates the stone/g)||[]).length===1);\\n\\nconsole.log(\\\"\\\\nB \\u2014 DUAL GEM: diamond shell + emerald interior\\\");\\nlet b=run({material:NACRE,gem:DIAMOND,gem2:EMERALD});\\nchk(\\\"no error\\\", !b.err); if(b.err)console.log(\\\"     \\\"+b.err);\\nchk(\\\"TWO gem blocks\\\", (b.src.match(/Q rotates the stone/g)||[]).length===2);\\nchk(\\\"shell guarded by ff\\\", b.src.includes(\\\"if (ff) {\\\"));\\nchk(\\\"interior guarded by !ff\\\", b.src.includes(\\\"if (!ff) {\\\"));\\nchk(\\\"diamond ior present (eta 0.41322)\\\", b.src.includes(\\\"0.41322\\\"));\\nchk(\\\"emerald ior present (eta 0.63291)\\\", b.src.includes(\\\"0.63291\\\"));\\nchk(\\\"longer than single-gem\\\", b.src.length > a.src.length);\\n\\nconsole.log(\\\"\\\\nC \\u2014 the 0.44.1 trap: gem2 with NO gem\\\");\\nlet c=run({material:NACRE,gem2:EMERALD});\\nchk(\\\"no error\\\", !c.err); if(c.err)console.log(\\\"     \\\"+c.err);\\nchk(\\\"gemHueRot IS declared\\\", c.src.includes(\\\"fn gemHueRot\\\"));\\nchk(\\\"gemSky IS declared\\\", c.src.includes(\\\"fn gemSky\\\"));\\nchk(\\\"exactly one gem block\\\", (c.src.match(/Q rotates the stone/g)||[]).length===1);\\n\\nconsole.log(\\\"\\\\nD \\u2014 hue offset\\\");\\nlet d=run({material:NACRE,gem:DIAMOND,gem2:EMERALD,gem2Hue:0.5});\\nchk(\\\"offset constant emitted\\\", d.src.includes(\\\"V.render3.x * 6.2831 + 3.14155\\\"));\\nchk(\\\"shell UNoffset\\\", d.src.includes(\\\"), V.render3.x * 6.2831);\\\"));\\n\\nconsole.log(\\\"\\\\nE \\u2014 gemBuff\\\");\\nlet e=run({material:NACRE,gem:DIAMOND,gem2:EMERALD,gemBuff:1.0});\\nchk(\\\"buff 1.0 emitted\\\", e.src.includes(\\\"select(1.0, 1.0, !ff)\\\"));\\nchk(\\\"default is 0.5\\\", b.src.includes(\\\"select(1.0, 0.5, !ff)\\\"));\\n\\nconsole.log(\\\"\\\\n\\\"+(bad?`FAIL \\u2014 ${bad} check(s)`:\\\"PASS \\u2014 all checks\\\"));\\nprocess.exit(bad?1:0);\\n\"");

const FILES = [...new Set(EDITS.map(e => e.file))];

console.log("apply_gem2.mjs - DUAL GEM (ZigWebGPU 0.47.0)\n");

for (const f of FILES) if (!fs.existsSync(f)) {
  bad(`target not found: ${f}`);
  console.log("\n  Run this from the repo root (C:\\Users\\billy\\Zigverse).");
  process.exit(1);
}

/* --- idempotency ------------------------------------------------------- */
const src0 = Object.fromEntries(FILES.map(f => [f, fs.readFileSync(f, "utf8")]));
const done = EDITS.filter(e => src0[e.file].includes(e.new)).length;
if (done === EDITS.length && fs.existsSync("test/gem2_ref.mjs")) {
  console.log("  ALREADY APPLIED - nothing to do.");
  process.exit(0);
}
if (done > 0) {
  bad(`partially applied (${done}/${EDITS.length}). Refusing to touch a half-patched tree.`);
  console.log("  git checkout -- " + FILES.join(" ") + "   then re-run.");
  process.exit(1);
}

/* --- GATE 1: every anchor unique, across every file --------------------- */
console.log("  GATE 1 - anchor uniqueness (" + EDITS.length + " edits, " + FILES.length + " files)");
let fail = false;
for (const f of FILES) {
  const mine = EDITS.filter(e => e.file === f);
  let bads = 0;
  for (const e of mine) { const n = src0[f].split(e.old).length - 1; if (n !== 1) { bads++; bad(`${f}  an anchor matched ${n} times`); } }
  if (!bads) ok(`${f.padEnd(24)} ${mine.length} anchors, 1 match each`);
  else fail = true;
}
if (fail) {
  console.log("\n  Nothing written. The tree has drifted from the commit this installer expects.");
  console.log("  Send me the current file and I will rebuild the anchors.");
  process.exit(1);
}

/* --- apply in memory ---------------------------------------------------- */
const out = { ...src0 };
for (const e of EDITS) out[e.file] = out[e.file].replace(e.old, e.new);

/* --- GATE 2: blast radius ----------------------------------------------- */
console.log("\n  GATE 2 - blast radius");
for (const f of FILES) {
  const a = src0[f].split("\n"), b = out[f].split("\n");
  const n = EDITS.filter(e => e.file === f).length;
  /* multiset difference, not a positional one: an insertion shifts every line
     below it, so comparing by index would report the whole rest of the file. */
  const bag = new Map(); for (const l of b) bag.set(l, (bag.get(l) || 0) + 1);
  let gone = 0; for (const l of a) { const c = bag.get(l) || 0; if (c) bag.set(l, c - 1); else gone++; }
  let added = 0; for (const c of bag.values()) added += c;
  if (gone > n * 3) { bad(`${f}  ${gone} original lines replaced for ${n} edits - too wide`); fail = true; }
  else ok(`${f.padEnd(24)} ${n} anchored edit(s) - ${gone} line(s) replaced, ${added} added, net +${b.length - a.length}`);
}

/* --- GATE 3: the emitted WGSL ------------------------------------------- */
console.log("\n  GATE 3 - the law is wired");
const zw = out["engine/zigwebgpu.js"];
const checks = [
  ["opts.gem2 read",                 zw.includes("const GEM2 = opts.gem2 || null;")],
  ["second gemBlock invocation",     zw.includes('gemBlock(GEM2, "if (!ff) ", GEM2HUE)')],
  ["helpers gated on GEM || GEM2",   zw.includes("if (GEM || GEM2) RENDER_SRC")],
  ["gemFace moot when gem2 set",     zw.includes('const GEMCOND = GEM2 ? "if (ff) "')],
  ["interior buff parameterised",    zw.includes("const GEMBUFF =")],
  ["VERSION 0.47.0",                 zw.includes('ZigWebGPU.VERSION = "0.47.0"')],
  ["species passes gem2",            out["species/sickleswarm.js"].includes("gem2: GEM2PROF")],
  ["host has GEM 2 picker",          out["zigverse_engine.html"].includes('id="gem2pick"')],
];
for (const [n, c] of checks) { if (c) ok(n); else { bad(n); fail = true; } }
if (fail) { console.log("\n  Nothing written."); process.exit(1); }

if (DRY) {
  console.log("\n  DRY RUN - all gates pass, nothing written. Re-run without --dry-run to apply.");
  process.exit(0);
}

/* --- write --------------------------------------------------------------- */
for (const f of FILES) {
  fs.writeFileSync(f + ".bak", src0[f], "utf8");
  fs.writeFileSync(f, out[f], "utf8");
  console.log(`\n  WRITTEN ${f}`);
  console.log(`          ${sha(src0[f]).slice(0, 16)}... -> ${sha(out[f]).slice(0, 16)}...`);
}
fs.mkdirSync("test", { recursive: true });
fs.writeFileSync("test/gem2_ref.mjs", GATE, "utf8");
console.log(`\n  WRITTEN test/gem2_ref.mjs  (${GATE.length} bytes)`);

console.log("\n  NEXT (do not skip):");
console.log("    node --check engine\\zigwebgpu.js");
console.log("    node --check species\\sickleswarm.js");
console.log("    node test\\gem2_ref.mjs            <- the new law's own gate");
console.log("    run all 46 test\\*_ref.mjs         <- the contract");
console.log("    node tools\\boot_gate.mjs          <- FIRST REAL GPU TEST");
console.log("    then your eyes, then LAND.cmd");
console.log("\n  Remove the .bak files before LAND.cmd (it runs git add -A).");
