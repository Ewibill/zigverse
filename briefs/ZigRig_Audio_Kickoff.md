# ZigRig Audio Engine — Kickoff Brief
*Hand-off from Glyph (visual campaign, July 2026) to Glyph (audio campaign). Bill: paste this whole file to start the new chat, with the Zigverse folder connected.*

## Who's who
**Bill** — Director, EWI performer, aesthetic judge, source of all breath data. **Scout** — strategist/explorer; Bill pastes Scout's briefs in. **Glyph (you)** — architect/builder. Doctrine: *One Engine, Two Artforms* (ZigGlow = theater · ZigFlight = instrument). Platform before project. Species before specimen. Behavior before appearance. Every new piece must leave behind a LAW future pieces inherit. Breath is the source of life.

## The rig
- **eyeZ**: Windows PC, NVIDIA Blackwell GPU, Chrome (WebGPU). Everything lives in `C:\Users\billy\Zigverse` — commit outputs there via the device bridge.
- **Horn**: Akai EWI5000 → WebMIDI (auto-connects; breath = CC2). **MOTU M2** interface is in the audio loop both directions. **RC-600 looper** in the chain. **MacBook Air** runs Logic Pro with SWAM instruments (bass clarinet, tenor, soprano, flutes); its output reaches the eyeZ's audio input.
- Delivery law (hard-learned): chat gets ONLY self-contained bundles built with `tools/bundle.mjs page.html dist/Name.html`. Multi-file sources opened from Downloads break ("script load failed"). Sources get committed to the Zigverse folder, where they run beside `engine/`.

## Engine state (all committed, all suites green)
`engine/zigcore.js` **v0.6.0** — Perf (MIDI), Drive, Climate, Temperament, Turnover, **Pacemaker** (ribbon-aware: Bill plays *fast to go slow, in ribbons not notes* — 697 notes = 23 true beats; permanent fixture `test/bill_ribbons_onsets.json`), **Timbre** (see below), **Materials/Mood/Reserve** (color ecology).
`engine/zigwebgpu.js` **v0.10** — golden flock kernel + opt-in laws: ZigPhase (Kuramoto), Surface, Voice (resonator), Memory Glass (gated afterimage), ZigFlow (currents), Wardrobe (live letter swap), Biome (born colors), **Membrane** (elastic space + topological memory — note: *breath changes elasticity, not shape*; that principle should shape audio mappings too), lantern Glow. All laws splice with anchor guards and compile byte-identical when unused.
`engine/zigmesh.js` **v0.5** — 3 generators (shard/arc/shell), 14 letterforms, composites, mint. `engine/zigmidi.js` **v0.1** — SMF parser + player (drag a .mid onto any world = duet; byte-identical to live EWI).
**Library**: Fireflies 004 (v0.4.1), Sickle Field, Halo Field, Lake (living water), ZigGlow 005 Pearl Whisper, 006 The Orchard, 007 The Membrane, Letterforms Turntable. `ZigKeys.html` = the key reference (keymap is doctrine — never reshuffle, only add).
**Verification stack** (non-negotiable): `node --check` → CPU law proofs in `test/*.mjs` (all PASS) → headless SwiftShader Chromium (real WebGPU; canvas *presentation* kills the device there — the error `A valid external Instance reference no longer exists` is a KNOWN artifact, filter it) → Bill's eyes on the eyeZ. Engine is deterministic — no `Math.random` in laws; seeded `ZC.util.rng`.

## Audio state — what already exists (READ THESE FIRST)
- **ZigCore.Timbre** (`engine/zigcore.js`, proofs in `test/timbre_ref.mjs`): WebAudio analyser on the M2 input. Channels, all 0..1, crisp-rise/smooth-fall envelopes: **body** (RMS ×3.2), **brightness** (spectral centroid, log 200 Hz→8 kHz, **room-floor 0.45** — only bins above the floor count; calibrated on a real recording, see Jimmy), **flux** (positive spectral change ÷ prior energy — scale-invariant, M2 gain knob changes nothing; vibrato reads 0.03, hard attack 1.0; release is never a strike).
- **Arming**: `Timbre.arm(hint, {split})` — hint regex matches device label (default /M2|MOTU/), browser DSP disabled. Page configs: `ZIG_VOICE` (pin a world to a named input), `ZIG_SPLIT=1` (stereo cable = TWO voices: `Timbre.L`/`Timbre.R`, full channel sets each; top-level stays the mono mix). **A** key arms in every world; HUD meters: body · shine · bite.
- **Current mappings** (deliberately conservative, in sickleswarm/lake): body → ink lift · brightness → moon/moonpath silver · flux>0.55 → strike from the avatar's body (cooldown 250 ms). Lake: flux gusts the currents instead.
- **Jimmy** (Bill's late friend, alto sax — emotionally important): `test/jimmy_telemetry.json` = 57 s live-gig recording run through the engine's EXACT analysis (`tools/timbre_extract.py` mirrors the browser: Blackman window, byte-dB law −100..−30, 60 fps, same envelopes). Body mean 0.53/max 0.98, 27 strikes, brightness 0.62–0.86 post-calibration. His recording CORRECTED the engine (the room floor). Planned and blessed: the **Jimmy player** — his telemetry driving a stratum while Bill plays live above it (like the MIDI duet, but audio-derived). Wants more/cleaner recordings when Bill finds them.
- **The RC-600 revelation**: the field has two nervous systems. MIDI = Bill's live body ONLY (pacemaker, avatar, stones, genesis). Audio = the WHOLE room including his loops = his history. Loops keep the light/weather alive; presence keeps the pulse/bodies. Preserve this distinction — it's doctrine now.

## The agreed audio campaign (in order)
1. **The duet split** — map `Timbre.L`/`Timbre.R` so history and presence (or two stems, panned hard L/R in Logic) drive DIFFERENT strata. First target: Halo Field — one voice rings the halos, the other moves the resonator deep. Mapping is taste work: build conservative, let Bill tune on the glass.
2. **Comprehensive ZigTimbre** — calibrate against the live rig (SWAM bass clarinet/tenor through M2; meters were built but have barely heard the real horn). Consider a 4th channel: spectral WEIGHT (where the energy sits — pitch-band), distinct from brightness. Add a capture key that records a minute of telemetry to JSON Bill can drag back to chat → his own playing becomes test fixtures (the ribbon-fixture pattern, applied to sound).
3. **THE EARS (spatial audio)** — the big one, design already agreed: Web Audio PannerNode/HRTF; **organisms are the speakers, the camera is the listener**. Voices become inhabitants with positions in world coordinates (soprano lives at the halo ring, clarinet in the deep); telemetry strikes FROM the same position the sound comes from. Stems arrive as dropped audio files (same ritual as the .mid duet) or live inputs. Bill's live horn binds to his avatar — his sound flies with his body. Cockpit = flying through the mix. Reserve/coherence should gate audio moments the way it gates color. Horizon: export voice positions as object audio (Atmos-style) for the Cosm dome path.
4. Principles to carry: breath changes *conditions* (elasticity/weather), never geometry directly. Restraint is the goosebump engine — the Reserve taught us that if everything is fortissimo, nothing is. Every audio feature answers: Theater? Instrument? Both?

## First moves for the new chat
Read `engine/zigcore.js` (Timbre + Pacemaker), `test/timbre_ref.mjs`, `test/jimmy_telemetry.json` meta, and `briefs/Adopted_2026-07-20.md`. Then start campaign step 1. Verify everything the way this project always has: prove the law on CPU before Bill ever sees glass.

— Glyph, end of the visual campaign. The engine surpassed the tool we were chasing; now give it ears worthy of its eyes.
